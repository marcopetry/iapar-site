import React from 'react';

//recebe uma função, o texto e um parâmetro que define uma cor
export default function Botao(props){

    return(
        <button>
            Cancelar
        </button>
    );
}